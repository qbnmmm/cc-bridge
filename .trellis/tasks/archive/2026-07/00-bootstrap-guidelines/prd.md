# Bootstrap cc-bridge Trellis Specs

## Goal

Create project-specific Trellis guidance from the repository's current Rust gateway, Vue dashboard, embedded craftls fork, tests, scripts, and documentation.

## Scope

- Configure Trellis packages for `gateway`, `web`, and `craftls`.
- Replace generated spec templates with source-backed package/layer guidance.
- Keep product source, manifests, build scripts, and existing project documentation unchanged.
- Configure both Codex and Claude Code integrations generated by `trellis init`.

## Evidence Read

- `AGENTS.md`, `CLAUDE.md`, `README.md`
- `Cargo.toml`, `web/package.json`, `craftls/Cargo.toml`
- `src/` architecture, protocol, store, cache, error, and test modules
- `web/src/` API, router, feature components, and UI primitives
- `scripts/`, `docker/`, `.github/workflows/release.yml`, benchmark tools

## Completion

- [x] Trellis packages map to the real repository components.
- [x] Gateway backend guidance covers architecture, Rust style, errors/logging, persistence, protocol rules, and tests.
- [x] Web guidance covers Vue structure, API contracts, UI conventions, and build validation.
- [x] Craftls guidance covers the fork boundary, fingerprint invariants, and tests.
- [x] Shared guides cover commands and cross-package changes.
- [x] Specs contain no generated placeholders or empty sections.
- [x] No product source code was modified.
